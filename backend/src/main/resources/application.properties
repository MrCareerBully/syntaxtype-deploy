spring.application.name=demo
# spring.datasource.url=jdbc:mysql://localhost:3306/syntaxtype

spring.datasource.driver-class-name=org.postgresql.Driver

# spring.datasource.username=root
# spring.datasource.password=lovejesus123day
# spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

logging.level.org.apache.catalina=DEBUG
logging.level.org.springframework.web.filter=DEBUG
logging.level.backend=DEBUG


#Render Configs
server.port=${PORT:8080}
# DB examples (set via Render env vars)
spring.datasource.url=jdbc:postgresql://${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update

# Hikari pool tuning (adjust for your plan)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.pool-name=HikariCP

# -----

#PostgreSQL Configuration below

# Database Configuration
# spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA and Hibernate
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false

# Server Error Handling
# server.error.include-stacktrace=never

# Logging
# logging.level.org.springframework.security=INFO
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql=TRACE

# JWT Secret
jwt.secret=YourSuperSecretKeyThatIsAtLeast256BitsLongAndRandom

# Admin User Details for initial setup
admin.username=admin
admin.email=admin@gmail.com
admin.password=admin123
logging.level.org.springframework.security=DEBUG

SPRING_DATASOURCE_URL=localhost:5432/syntaxtype
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=password

# CORS Configuration
FRONTEND_URL=${RENDER_FRONTEND_URL:localhost:3000}